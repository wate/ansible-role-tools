---
- name: Assert installed ripgrep
  ansible.builtin.assert:
    that: ansible_facts.packages['ripgrep'] is defined
    fail_msg: "ripgrep package is not installed"
    success_msg: "ripgrep package is installed"
