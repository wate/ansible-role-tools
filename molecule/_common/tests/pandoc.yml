---
- name: Assert installed pandoc
  ansible.builtin.assert:
    that:
      - ansible_facts.packages['pandoc'] is defined
      - ansible_facts.packages['pandoc'][0]['origin'] != 'Debian'
    fail_msg: "pandoc package is not installed"
    success_msg: "pandoc package is installed"
