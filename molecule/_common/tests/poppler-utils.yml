---
- name: Assert installed poppler-utils
  ansible.builtin.assert:
    that: ansible_facts.packages['poppler-utils'] is defined
    fail_msg: "poppler-utils package is not installed"
    success_msg: "poppler-utils package is installed"
