---
- name: Assert installed plantuml
  ansible.builtin.assert:
    that: ansible_facts.packages['plantuml'] is defined
    fail_msg: "plantuml package is not installed"
    success_msg: "plantuml package is installed"
