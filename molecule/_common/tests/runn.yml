---
- name: Assert installed runn
  ansible.builtin.assert:
    that: ansible_facts.packages['runn'] is defined
    fail_msg: "runn package is not installed"
    success_msg: "runn package is installed"
