---
- name: Assert installed libxml2-utils
  ansible.builtin.assert:
    that: ansible_facts.packages['libxml2-utils'] is defined
    fail_msg: "libxml2-utils package is not installed"
    success_msg: "libxml2-utils package is installed"
