---
- name: Assert installed tree
  ansible.builtin.assert:
    that: ansible_facts.packages['tree'] is defined
    fail_msg: "tree package is not installed"
    success_msg: "tree package is installed"
