---
- name: Assert installed mecab
  ansible.builtin.assert:
    that: ansible_facts.packages['mecab'] is defined
    fail_msg: "mecab package is not installed"
    success_msg: "mecab package is installed"
