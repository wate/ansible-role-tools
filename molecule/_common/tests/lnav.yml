---
- name: Assert installed lnav
  ansible.builtin.assert:
    that: ansible_facts.packages['lnav'] is defined
    fail_msg: "lnav package is not installed"
    success_msg: "lnav package is installed"
