---
- name: Assert installed bottom
  ansible.builtin.assert:
    that: ansible_facts.packages['bottom'] is defined
    fail_msg: "bottom package is not installed"
    success_msg: "bottom package is installed"
